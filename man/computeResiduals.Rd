% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeResiduals.R
\name{computeResiduals}
\alias{computeResiduals}
\title{Compute standardized residuals and p-values from a chi-squared test}
\usage{
computeResiduals(sub, col1 = "AgeGroup", col2 = "PrimarySymptom")
}
\arguments{
\item{sub}{A data frame containing the variables to test.}

\item{col1}{A character string specifying the first variable (e.g., row variable).}

\item{col2}{A character string specifying the second variable (e.g., column variable).}
}
\value{
A data frame with columns: \code{col1}, \code{col2}, \code{resid}, \code{pval}, and \code{label}.
}
\description{
This function performs a chi-squared test on two categorical variables from a data frame,
and returns a data frame of standardized residuals, associated p-values, and formatted labels.
}
\examples{
data(patients)
sub <- preProcess(patients, v1 = "AgeGroup", v2 = "PrimarySymptom")
residuals <- computeResiduals(sub, col1 = "AgeGroup", col2 = "PrimarySymptom")
head(residuals)

}
